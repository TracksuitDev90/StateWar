// Railways: player-built connections between any two owned states.
// A rail is either built or not — no upgrade tiers.
// Once built, units can move instantly between the two endpoints.

export const RAIL_BUILD_COST = 5;
